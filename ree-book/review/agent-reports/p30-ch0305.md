# P30 — Chapters 3 and 5

Files owned and edited:

- `src/03-solvent-extraction-fundamentals.md`
- `src/05-hydrometallurgical-leaching.md`

No other source file was touched. `references.bib` was not edited, and no
`review/bib-additions/p30-ch0305.bib` was needed (see "References" below).

## Job 1 — indented blocks

Blocks were enumerated with a detector that separates true four-space code
blocks from list-continuation indentation, so the counts are exact rather than
eyeballed.

| Chapter | Indented blocks found | → display math | → fenced (```) | Remaining indented |
|---|---|---|---|---|
| 03 | 24 | 23 | 1 | 0 |
| 05 | 26 | 26 | 0 | 0 |

Chapter 3's 23 conversions produced 23 `$$` blocks. Chapter 5's 26 conversions
produced 33 `$$` blocks, because several source blocks held more than one
equation (e.g. a decomposition reaction plus its subsequent dissolution step)
and each was given its own display block.

The single fenced block in chapter 3 is the ASCII pH-swing loop diagram near
the chapter summary (arrows and indentation carrying the meaning, not an
equation). It is now a ```` ```text ```` fence.

Chapter 5 had several blocks that mixed an equation with an embedded
`Conditions:` listing. Those were split: the equation became display math and
the listing became `**Conditions**:` plus a bullet list, which is the
convention the chapter already uses elsewhere. No fence was warranted.

Style notes on the LaTeX: only `\mathrm`, `\frac`/`\dfrac`/`\tfrac`, `\log`,
`\ln`, `\sum`, `\Delta`, `\beta`, `\partial`, `\approx`,
`\rightleftharpoons`, `\rightarrow`, `\uparrow`/`\downarrow`, `\cdot`,
`\times`, `\left(`/`\right)`, `\begin{aligned}`, `\text`, `\quad`, `\,` are
used. No macros were invented. Chemical formulae inside math use `\mathrm`;
Unicode formulae in ordinary prose (H₂SO₄, (NH₄)₂SO₄) were left alone.

The `fig-logd-vs-ph` caption still carries its equation as inline code
(`` `log D = log Kₑₓ + 3 log[(HL)₂] + 3 pH` ``). That was left as-is under the
"leave figure directives and captions intact" rule; flag it if captions are in
scope for a later pass.

## Job 2 — redundant re-derivation in chapter 5

Chapter 5 shrank from 1660 to about 1584 lines despite gaining ~80 lines of
math markup, so the prose cuts are larger than the net number suggests.

| Cut | Replaced with | Pointed to |
|---|---|---|
| Ion-adsorption clay leaching section, 184 → 92 lines (deposit genesis, REE speciation on clay, geographic/geological background, duplicated kinetics) | Exchange equation, the grade and HREE-share facts the chapter's own mass balance needs, lixiviant and contacting practice, recovery from dilute liquor | `[](#ion-adsorption-clays)` |
| Duplicate shrinking-core kinetics block inside the clay section | Pointer to the chapter's own kinetics section, which now carries the new anchor `(leaching-kinetics-models)=` | `[](#leaching-kinetics-models)` |
| `### Principles of Bioleaching` + `### Microorganisms Used` (fungus/bacterium catalogue) | Two paragraphs and one schematic equation | `[](#biological-and-biomimetic-separations)` |
| `### Advantages and Limitations` + `**Current status**` bullet lists | One paragraph | `[](#biological-and-biomimetic-separations)` |
| `### Urban Mining` | Six lines of prose | `[](#recycling-and-urban-mining)` |
| `### Major Environmental Concerns` (five-item nested list) | One paragraph | `[](#environment-techno-economics-and-life-cycle)`, `[](#ion-adsorption-clays)` |
| `### Waste Minimization Strategies` | Prose, keeping the leach-specific byproduct list | `[](#environment-techno-economics-and-life-cycle)` |
| Ionic-liquid bullet list | Prose | `[](#biological-and-biomimetic-separations)`, `[](#environment-techno-economics-and-life-cycle)` |

Every owning chapter was read before cutting, to confirm it actually contains
the material. `grep -rn "#<label>" src/` was run before touching any anchor:
`src/06-ion-adsorption-clays.md:19` links into chapter 5's
`(ion-adsorption-clay-leaching-the-gentle-approach)=`, so that anchor was
preserved verbatim while its section was condensed. All 14 cross-reference
targets in the two files resolve to exactly one anchor or figure `:name:` in
`src/`.

Deliberately **not** cut, because the owning chapter does not have it:

- Chapter 6 lacks: FeSO₄ reduction leaching (`xiao2016reduction`); NaCl and
  organic-acid lixiviants (`wang2017effects`); heap geometry and irrigation
  practice; the 200–1000 mg/L PLS concentration; the `shi2022column`
  low-ammonium column result (0.2 % → 93 % vs 1.0 % → 85 %); shrinking-core
  kinetics applied to clay leaching (`long2019kinetics`).
- Chapter 11 lacks: the acidolysis / redoxolysis / complexolysis taxonomy;
  *Acidithiobacillus ferrooxidans*; and the critical reading of the primary
  bioleaching literature (Brisson, Wang, Corbett). The two ch.-5-unique
  sections `### Bioleaching Performance: What the Primary Literature Actually
  Reports` and `### Indigenous Microorganism Enhancement` were kept whole.
- Chapter 17 lacks the byproduct-market list (F → HF/AlF₃, phosphate →
  fertiliser, Fe → pigment, Th → nuclear fuel), so it stayed in chapter 5.
- The DES paragraph stayed: it is chapter 5's own correction of a common
  overclaim, citing `ni2023sustainable` on thorium co-extraction.

Closing these gaps in chapters 6, 11 and 17 would let the corresponding
chapter-5 passages shrink further; that is a follow-up, not part of P30.

## Source-document seam near 05:1040

The assignment flagged a leftover "the chemistry fundamentals review" phrase.
It is not present. `git log -S` shows it was at line 1053 and was already
repaired in commit `ffcc639`; the sentence now reads "This solution is the feed
for the solvent extraction circuit described in
[](#solvent-extraction-fundamentals)." No action taken.

## References

A citation-key set diff against `HEAD` (using `git show HEAD:./src/...`, note
the `./`) shows **zero keys added and zero removed** in either file. The
condensed sections kept their citations. So `references.md`'s "Cited in
[](#hydrometallurgical-leaching)" lines remain accurate, no entry was orphaned,
and no new bibliography entries were required. `review/bib-additions/p30-ch0305.bib`
was therefore not created.

## Validation (no build run)

1. Indented-block detector re-run: 0 remaining in both files.
2. LaTeX check over all 56 math blocks: no macro outside the allowed set;
   braces, brackets and parentheses balanced in every block.
3. `$$` delimiter check: every opener preceded by a blank line, every closer
   followed by one, all balanced. Reported 0 issues for both files.
4. All 14 cross-reference targets resolve to exactly one anchor in `src/`.
5. Figure directives, `:name:` values and their references untouched
   (`fig-leaching-flowsheet`, `fig-logd-vs-ph`, `fig-cascade`).

## Left unfixed

The seven self-conflicting number pairs listed in the assignment as out of
scope are all still present, and none of them collapsed naturally out of the
de-duplication: ore grade (5–15 % vs 7–9 % vs 10–30 %), bastnäsite leach acid
(4–8 M HCl in prose vs 2–6 M in the table), clay lixiviant strength, the eight
leach residence-time ranges, ThO₂ ("0–12 %" vs "0.1–12 wt %"), SX feed pH
(0.5–2.0 vs free acid 0.1–1.0 M), and monazite concentrate grade (55 % vs
50–70 %). No resolution was invented.

Two things did become single-statement rather than repeated, which is adjacent
but not a resolution: the shrinking-core model is now stated once (in
`#leaching-kinetics-models`) instead of twice with different symbol sets
(`k_app` vs `k_s`/`k_d`), and the (NH₄)₂SO₄ / MgSO₄ lixiviant strengths are now
given in one place instead of two.
